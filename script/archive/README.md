# Archived Scripts

These scripts were used for manual parameter testing before the autoinstall variant suite (`os/ubuntu/variants/`) was created. They are superseded by:

- `apply-test-a.sh` → `os/ubuntu/variants/autoinstall-A-display-only.yaml`
- `apply-test-b-1080p.sh` → `os/ubuntu/variants/autoinstall-B-display-firmware.yaml`

#cloud-config
autoinstall:
  version: 1

  # =========================================================================
  # VARIANT D: labwc + pixman (Wayland, Stacking, Zero GFX Ring)
  #
  # PURPOSE: Modern Wayland stacking compositor with CPU-only rendering.
  #   labwc is a wlroots-based Openbox-like stacking WM. With pixman
  #   renderer, ALL compositing is done on CPU — zero GFX ring submissions.
  #   Crash-proof even if firmware fix is incomplete.
  #
  # WHAT THIS TESTS:
  #   - Can a Wayland compositor with pixman rendering fully avoid
  #     the GFX ring crash loop?
  #   - Is labwc a viable production desktop for ML workstation use?
  #
  # RISK: LOWEST — pixman renderer means zero GPU compositing.
  #   Even with broken DMCUB, the GFX ring is never used for display.
  #   The two-condition crash model (DCN stall + GFX pressure) is
  #   broken by eliminating GFX pressure entirely.
  #
  # PREREQUISITE: Place firmware blobs on USB (same as Variant B).
  #
  # DESKTOP: labwc + waybar + foot terminal + thunar file manager
  #   Familiar stacking window management (like Windows/XFCE).
  # =========================================================================

  interactive-sections: []

  locale: en_US.UTF-8
  keyboard:
    layout: us
    variant: ""
  timezone: America/New_York

  refresh-installer:
    update: true
    channel: stable

  source:
    id: ubuntu-desktop-minimal
    search_drivers: false

  kernel:
    package: linux-generic-hwe-24.04

  network:
    version: 2
    ethernets:
      eno1:
        dhcp4: true
        dhcp6: false
        optional: true

  proxy: ""

  apt:
    preserve_sources_list: false
    primary:
      - arches: [amd64]
        uri: http://archive.ubuntu.com/ubuntu
    security:
      - arches: [amd64]
        uri: http://security.ubuntu.com/ubuntu

  storage:
    version: 2
    config:
      - id: nvme-disk
        type: disk
        ptable: gpt
        serial: Samsung_SSD_990_PRO_2TB_S73WNJ0W206133V_1
        preserve: true
        wipe: none
        grub_device: false

      - id: efi-windows-part
        type: partition
        device: nvme-disk
        number: 1
        size: 100M
        preserve: true

      - id: msr-part
        type: partition
        device: nvme-disk
        number: 2
        size: 16M
        preserve: true

      - id: windows-part
        type: partition
        device: nvme-disk
        number: 3
        size: 975345549312
        preserve: true

      - id: winre-part
        type: partition
        device: nvme-disk
        number: 4
        size: 888M
        offset: 1999467708416
        preserve: true

      - id: efi-part
        type: partition
        device: nvme-disk
        size: 1G
        offset: 975468232704
        flag: esp
        grub_device: true

      - id: efi-format
        type: format
        volume: efi-part
        fstype: fat32
        label: EFI-UBUNTU

      - id: efi-mount
        type: mount
        device: efi-format
        path: /boot/efi

      - id: boot-part
        type: partition
        device: nvme-disk
        size: 2G
        offset: 976541974528

      - id: boot-format
        type: format
        volume: boot-part
        fstype: ext4
        label: BOOT

      - id: boot-mount
        type: mount
        device: boot-format
        path: /boot

      - id: swap-part
        type: partition
        device: nvme-disk
        size: 64G
        offset: 978689458176

      - id: swap-format
        type: format
        volume: swap-part
        fstype: swap
        label: SWAP

      - id: swap-mount
        type: mount
        device: swap-format
        path: none

      - id: root-part
        type: partition
        device: nvme-disk
        size: 200G
        offset: 1047408934912

      - id: root-format
        type: format
        volume: root-part
        fstype: ext4
        label: ROOT

      - id: root-mount
        type: mount
        device: root-format
        path: /

      - id: home-part
        type: partition
        device: nvme-disk
        size: -1
        offset: 1262157299712

      - id: home-format
        type: format
        volume: home-part
        fstype: ext4
        label: HOME

      - id: home-mount
        type: mount
        device: home-format
        path: /home

  identity:
    hostname: vortex
    realname: Abraham
    username: abraham
    password: "$6$tgmHIWX35Qendhwj$hSQ0dDsaUy2zJwnh8uRoMBEHybDjWXsmnjgi2fcECzmAZGrMfpuSDnOnTVG/uJ/G23Yf403i//i984JlbFvhy1"

  ssh:
    install-server: true
    allow-pw: true
    authorized-keys: []

  drivers:
    install: false

  codecs:
    install: true

  updates: security

  packages:
    # --- System essentials ---
    - ubuntu-minimal
    - ubuntu-standard
    - apt-transport-https
    - ca-certificates
    - curl
    - wget
    - gnupg
    - software-properties-common
    - lsb-release
    - unzip
    - zstd
    - pciutils
    - usbutils
    - dmidecode
    - lm-sensors
    - cpufrequtils
    - acpi
    - smartmontools
    - ethtool
    - net-tools
    - build-essential
    - dkms
    - gcc
    - g++
    - make
    - linux-headers-generic
    - linux-headers-generic-hwe-24.04

    # --- GPU/display stack (Xwayland support) ---
    - mesa-utils
    - mesa-vulkan-drivers
    - libgl1-mesa-dri
    - libgl1
    - libglx-mesa0
    - libdrm-dev
    - libdrm-amdgpu1
    - xserver-xorg
    - xserver-xorg-core
    - xserver-xorg-video-amdgpu
    - x11-utils
    - x11-xserver-utils
    - vainfo
    - vulkan-tools
    - clinfo

    # --- labwc Wayland desktop (replaces XFCE) ---
    - labwc
    - waybar
    - foot
    - wl-clipboard
    - xdg-desktop-portal-wlr
    - swaybg
    - swayidle
    - swaylock
    - mako-notifier
    - thunar
    - xwayland
    - wofi

    # --- LightDM (launches Wayland session) ---
    - lightdm
    - lightdm-gtk-greeter
    - lightdm-gtk-greeter-settings

    # --- Firmware & kernel tools ---
    - linux-firmware
    - fwupd
    - initramfs-tools
    - mokutil
    - efibootmgr
    - amd64-microcode

    # --- Networking ---
    - openssh-server
    - openssh-client
    - network-manager
    - firewalld
    - rsync
    - tmux
    - screen

    # --- Storage & monitoring ---
    - nvme-cli
    - hdparm
    - parted
    - gdisk
    - e2fsprogs
    - dosfstools
    - htop
    - iotop
    - sysstat
    - strace
    - inxi
    - linux-tools-generic-hwe-24.04
    - git
    - vim
    - nano
    - jq
    - tree
    - xxd
    - bc

  snaps: []

  early-commands:
    - 'echo "=== Variant D: labwc+pixman — $(date -u +%Y-%m-%dT%H:%M:%SZ) ===" > /tmp/autoinstall-hw.log'
    - lspci -nn >> /tmp/autoinstall-hw.log 2>&1 || true
    - dmidecode -t system >> /tmp/autoinstall-hw.log 2>&1 || true
    - cat /proc/cpuinfo | head -30 >> /tmp/autoinstall-hw.log 2>&1 || true

  late-commands:

    # =========================================================================
    # 1. GRUB — Same as Variant B (AMD-only, no NVIDIA params)
    # =========================================================================
    - >-
      curtin in-target -- bash -c '
      GRUB_PARAMS="quiet splash amdgpu.sg_display=0 amdgpu.dcdebugmask=0x18 amdgpu.ppfeaturemask=0xfffd7fff amdgpu.gpu_recovery=1 amdgpu.seamless=1 amdgpu.lockup_timeout=30000 pcie_aspm=off iommu=pt processor.max_cstate=1 amd_pstate=active nogpumanager initcall_blacklist=simpledrm_platform_driver_init";
      grep -q "^GRUB_CMDLINE_LINUX_DEFAULT=" /etc/default/grub
      && sed -i "s|^GRUB_CMDLINE_LINUX_DEFAULT=.*|GRUB_CMDLINE_LINUX_DEFAULT=\"${GRUB_PARAMS}\"|" /etc/default/grub
      || echo "GRUB_CMDLINE_LINUX_DEFAULT=\"${GRUB_PARAMS}\"" >> /etc/default/grub'

    - curtin in-target -- bash -c 'grep -q "^GRUB_TIMEOUT=" /etc/default/grub && sed -i "s|^GRUB_TIMEOUT=.*|GRUB_TIMEOUT=5|" /etc/default/grub || echo "GRUB_TIMEOUT=5" >> /etc/default/grub'
    - curtin in-target -- bash -c 'grep -q "^GRUB_TIMEOUT_STYLE=" /etc/default/grub && sed -i "s|^GRUB_TIMEOUT_STYLE=.*|GRUB_TIMEOUT_STYLE=menu|" /etc/default/grub || echo "GRUB_TIMEOUT_STYLE=menu" >> /etc/default/grub'
    - curtin in-target -- update-grub

    # =========================================================================
    # 2. MODPROBE — amdgpu only (no NVIDIA in this variant)
    # =========================================================================
    - |
      cat > /target/etc/modprobe.d/amdgpu.conf << 'MODPROBE_AMDGPU'
      # Variant D: AMD + firmware fix (Wayland/pixman)
      options amdgpu sg_display=0
      options amdgpu ppfeaturemask=0xfffd7fff
      options amdgpu dcdebugmask=0x18
      options amdgpu gpu_recovery=1
      options amdgpu lockup_timeout=30000
      options amdgpu seamless=1
      options amdgpu dc=1
      options amdgpu audio=1
      MODPROBE_AMDGPU

    - |
      cat > /target/etc/modprobe.d/blacklist-nouveau.conf << 'BLACKLIST_NOUVEAU'
      blacklist nouveau
      blacklist nova_core
      blacklist lbm-nouveau
      alias nouveau off
      alias lbm-nouveau off
      BLACKLIST_NOUVEAU

    # =========================================================================
    # 3. INITRAMFS — AMD only
    # =========================================================================
    - |
      cat > /target/etc/initramfs-tools/modules << 'INITRAMFS_MODULES'
      # Variant D: AMD-only (no NVIDIA in initramfs)
      amdgpu
      INITRAMFS_MODULES

    - |
      cat > /target/etc/modules-load.d/gpu.conf << 'MODULES_LOAD'
      amdgpu
      MODULES_LOAD

    # =========================================================================
    # 4. FIRMWARE UPDATE FROM USB (same as Variant B)
    # =========================================================================
    - |
      echo "=== Variant D: Installing firmware from USB ===" >> /tmp/autoinstall-hw.log
      FIRMWARE_FOUND=false
      for usbpath in /cdrom /media/cdrom /mnt/usb; do
        FWDIR="${usbpath}/UbuntuAutoInstall/firmware/amdgpu"
        if [ -d "$FWDIR" ] && ls "$FWDIR"/*.bin >/dev/null 2>&1; then
          echo "Found firmware at: $FWDIR" >> /tmp/autoinstall-hw.log
          FIRMWARE_FOUND=true

          BACKUP="/target/var/log/ml-workstation-setup/firmware-backup"
          mkdir -p "$BACKUP"
          cp -a /target/lib/firmware/amdgpu/dcn_3_1_5_dmcub.bin* "$BACKUP/" 2>/dev/null || true
          cp -a /target/lib/firmware/amdgpu/psp_13_0_5_*.bin* "$BACKUP/" 2>/dev/null || true
          cp -a /target/lib/firmware/amdgpu/gc_10_3_6_*.bin* "$BACKUP/" 2>/dev/null || true

          for blob in "$FWDIR"/*.bin; do
            bname=$(basename "$blob")
            cp "$blob" "/target/lib/firmware/amdgpu/$bname"
            if zstd -f "/target/lib/firmware/amdgpu/$bname" \
                 -o "/target/lib/firmware/amdgpu/${bname}.zst"; then
              rm -f "/target/lib/firmware/amdgpu/$bname"
              echo "  Installed: $bname -> ${bname}.zst" >> /tmp/autoinstall-hw.log
            else
              echo "  ERROR: zstd failed for $bname — left as bare .bin" >> /tmp/autoinstall-hw.log
            fi
          done

          ls -la /target/lib/firmware/amdgpu/dcn_3_1_5_dmcub.bin* >> /tmp/autoinstall-hw.log 2>&1
          break
        fi
      done

      if [ "$FIRMWARE_FOUND" = "false" ]; then
        echo "WARNING: Firmware blobs NOT found on USB!" >> /tmp/autoinstall-hw.log
        echo "Expected at: <USB>/UbuntuAutoInstall/firmware/amdgpu/*.bin" >> /tmp/autoinstall-hw.log
        echo "Variant D uses pixman — should still work without firmware fix" >> /tmp/autoinstall-hw.log
      fi

    # =========================================================================
    # 5. LightDM — launches labwc Wayland session
    #
    # LightDM GTK greeter runs on X11 briefly (negligible GPU impact),
    # then launches the Wayland user session via labwc-pixman.desktop.
    # =========================================================================
    - curtin in-target -- bash -c 'echo "/usr/sbin/lightdm" > /etc/X11/default-display-manager'
    - curtin in-target -- dpkg-reconfigure -f noninteractive lightdm || true

    - mkdir -p /target/etc/lightdm/lightdm.conf.d
    - |
      cat > /target/etc/lightdm/lightdm.conf.d/50-labwc-session.conf << 'LIGHTDM_LABWC'
      [Seat:*]
      user-session=labwc-pixman
      greeter-session=lightdm-gtk-greeter
      LIGHTDM_LABWC

    # Remove GNOME/XFCE session files
    - curtin in-target -- bash -c 'rm -f /usr/share/xsessions/ubuntu.desktop 2>/dev/null || true'
    - curtin in-target -- bash -c 'rm -f /usr/share/xsessions/ubuntu-xorg.desktop 2>/dev/null || true'
    - curtin in-target -- bash -c 'dpkg-divert --rename --divert /usr/share/xsessions/gnome.desktop.disabled /usr/share/xsessions/gnome.desktop 2>/dev/null || true'
    - curtin in-target -- bash -c 'dpkg-divert --rename --divert /usr/share/xsessions/gnome-xorg.desktop.disabled /usr/share/xsessions/gnome-xorg.desktop 2>/dev/null || true'

    # =========================================================================
    # 6. labwc Wayland session file
    #
    # This .desktop file goes in wayland-sessions/ (not xsessions/).
    # LightDM 1.30+ supports Type=wayland sessions.
    # =========================================================================
    - mkdir -p /target/usr/share/wayland-sessions
    - |
      cat > /target/usr/share/wayland-sessions/labwc-pixman.desktop << 'LABWC_SESSION'
      [Desktop Entry]
      Name=labwc (pixman)
      Comment=labwc stacking Wayland compositor with CPU-only rendering
      Exec=env WLR_RENDERER=pixman labwc
      Type=Application
      DesktopNames=wlroots
      LABWC_SESSION

    # =========================================================================
    # 7. labwc configuration
    #
    # rc.xml: keybinds + window rules (Openbox-like XML format)
    # environment: WLR_RENDERER=pixman + XDG vars
    # autostart: launch waybar + mako
    # =========================================================================
    - mkdir -p /target/etc/xdg/labwc

    - |
      cat > /target/etc/xdg/labwc/rc.xml << 'LABWC_RC'
      <?xml version="1.0"?>
      <labwc_config>
        <core>
          <gap>4</gap>
        </core>
        <theme>
          <name>Adwaita</name>
          <cornerRadius>8</cornerRadius>
        </theme>
        <keyboard>
          <!-- Window management -->
          <keybind key="A-F4">
            <action name="Close"/>
          </keybind>
          <keybind key="A-Tab">
            <action name="NextWindow"/>
          </keybind>
          <keybind key="A-Return">
            <action name="Execute"><command>foot</command></action>
          </keybind>
          <keybind key="W-e">
            <action name="Execute"><command>thunar</command></action>
          </keybind>
          <keybind key="W-Return">
            <action name="Execute"><command>foot</command></action>
          </keybind>
          <keybind key="W-d">
            <action name="Execute"><command>wofi --show drun</command></action>
          </keybind>
          <!-- Workspace switching -->
          <keybind key="W-1">
            <action name="GoToDesktop"><to>1</to></action>
          </keybind>
          <keybind key="W-2">
            <action name="GoToDesktop"><to>2</to></action>
          </keybind>
          <keybind key="W-3">
            <action name="GoToDesktop"><to>3</to></action>
          </keybind>
          <keybind key="W-4">
            <action name="GoToDesktop"><to>4</to></action>
          </keybind>
          <!-- Snap to edges -->
          <keybind key="W-Left">
            <action name="SnapToEdge"><direction>left</direction></action>
          </keybind>
          <keybind key="W-Right">
            <action name="SnapToEdge"><direction>right</direction></action>
          </keybind>
          <keybind key="W-Up">
            <action name="ToggleMaximize"/>
          </keybind>
          <!-- Lock screen -->
          <keybind key="W-l">
            <action name="Execute"><command>swaylock -f -c 1a1a2e</command></action>
          </keybind>
        </keyboard>
        <mouse>
          <default/>
        </mouse>
        <desktops number="4"/>
      </labwc_config>
      LABWC_RC

    - |
      cat > /target/etc/xdg/labwc/environment << 'LABWC_ENV'
      WLR_RENDERER=pixman
      XDG_CURRENT_DESKTOP=wlroots
      DRI_PRIME=0
      MOZ_ENABLE_WAYLAND=1
      QT_QPA_PLATFORM=wayland
      _JAVA_AWT_WM_NONREPARENTING=1
      LABWC_ENV

    - |
      cat > /target/etc/xdg/labwc/autostart << 'LABWC_AUTOSTART'
      # Status bar
      waybar &
      # Notification daemon
      mako &
      # Idle management (lock after 5 min, suspend after 15)
      swayidle -w \
        timeout 300 'swaylock -f -c 1a1a2e' \
        timeout 900 'systemctl suspend' \
        before-sleep 'swaylock -f -c 1a1a2e' &
      LABWC_AUTOSTART
    - chmod 0755 /target/etc/xdg/labwc/autostart

    # =========================================================================
    # 8. Disable unwanted services
    # =========================================================================
    - curtin in-target -- systemctl disable gpu-manager.service || true
    - curtin in-target -- systemctl mask gpu-manager.service || true
    - curtin in-target -- systemctl disable gdm3.service || true
    - curtin in-target -- systemctl mask gdm3.service || true
    - curtin in-target -- systemctl disable switcheroo-control.service || true
    - curtin in-target -- systemctl mask switcheroo-control.service || true
    - curtin in-target -- systemctl disable gnome-initial-setup-first-login.service || true
    - curtin in-target -- systemctl mask gnome-initial-setup-first-login.service || true
    - curtin in-target -- bash -c 'systemctl --global disable org.gnome.Shell@x11.service 2>/dev/null || true'
    - curtin in-target -- bash -c 'systemctl --global disable org.gnome.Shell@wayland.service 2>/dev/null || true'

    # =========================================================================
    # 9. NO Xorg config needed — labwc is Wayland-native
    #
    # Xwayland is available for X11 apps but doesn't need xorg.conf.d.
    # Do NOT create 10-amdgpu-primary.conf (no Xorg server for display).
    # =========================================================================

    # =========================================================================
    # 10. Environment
    # =========================================================================
    - mkdir -p /target/etc/environment.d
    - |
      cat > /target/etc/environment.d/50-gpu-display.conf << 'GPU_DISPLAY'
      WLR_RENDERER=pixman
      XDG_CURRENT_DESKTOP=wlroots
      DRI_PRIME=0
      GPU_DISPLAY

    - |
      cat > /target/etc/environment.d/90-mutter-kms.conf << 'MUTTER_KMS'
      MUTTER_DEBUG_KMS_THREAD_TYPE=user
      MUTTER_KMS

    # =========================================================================
    # 11. Diagnostics and verification
    # =========================================================================
    - mkdir -p /target/var/log/ml-workstation-setup

    - |
      for usbpath in /cdrom /media/cdrom /mnt/usb; do
        if [ -f "${usbpath}/UbuntuAutoInstall/script/diag-v2/diagnostic-enhanced.sh" ]; then
          cp "${usbpath}/UbuntuAutoInstall/script/diag-v2/diagnostic-enhanced.sh" /target/usr/local/bin/diagnostic-enhanced.sh
          chmod 0755 /target/usr/local/bin/diagnostic-enhanced.sh
          break
        fi
      done

    - |
      for usbpath in /cdrom /media/cdrom /mnt/usb; do
        if [ -f "${usbpath}/UbuntuAutoInstall/script/diag-v2/verify-boot.sh" ]; then
          cp "${usbpath}/UbuntuAutoInstall/script/diag-v2/verify-boot.sh" /target/usr/local/bin/verify-boot.sh
          chmod 0755 /target/usr/local/bin/verify-boot.sh
          break
        fi
      done

    - |
      cat > /target/etc/systemd/system/ml-boot-verify.service << 'BOOT_VERIFY'
      [Unit]
      Description=ML Workstation Boot Verification (Variant D: labwc+pixman)
      After=multi-user.target display-manager.service
      Wants=display-manager.service

      [Service]
      Type=oneshot
      ExecStartPre=/bin/sleep 30
      ExecStart=/usr/local/bin/verify-boot.sh --variant D --auto
      RemainAfterExit=yes
      StandardOutput=journal+console

      [Install]
      WantedBy=multi-user.target
      BOOT_VERIFY
    - curtin in-target -- systemctl enable ml-boot-verify.service

    - |
      cat > /target/etc/systemd/system/ml-diagnostic-capture.service << 'DIAG_CAPTURE'
      [Unit]
      Description=ML Workstation Diagnostic Capture
      After=multi-user.target display-manager.service

      [Service]
      Type=oneshot
      ExecStartPre=/bin/sleep 60
      ExecStart=/bin/bash -c 'if [ -x /usr/local/bin/diagnostic-enhanced.sh ]; then /usr/local/bin/diagnostic-enhanced.sh --auto; fi'
      RemainAfterExit=yes

      [Install]
      WantedBy=multi-user.target
      DIAG_CAPTURE
    - curtin in-target -- systemctl enable ml-diagnostic-capture.service

    # =========================================================================
    # 12. Services
    # =========================================================================
    - curtin in-target -- systemctl enable ssh.service
    - curtin in-target -- systemctl enable fstrim.timer
    - curtin in-target -- systemctl enable NetworkManager.service
    - curtin in-target -- systemctl disable cups.service || true
    - curtin in-target -- systemctl disable avahi-daemon.service || true
    - curtin in-target -- systemctl disable ModemManager.service || true
    - curtin in-target -- systemctl disable bluetooth.service || true
    - curtin in-target -- systemctl disable wpa_supplicant.service || true

    - curtin in-target -- usermod -aG video,render,sudo abraham

    # =========================================================================
    # 13. Hardware log from early-commands
    # =========================================================================
    - cp /tmp/autoinstall-hw.log /target/var/log/ml-workstation-setup/autoinstall-hw.log 2>/dev/null || true

    # =========================================================================
    # 14. REBUILD INITRAMFS — MUST BE LAST
    # =========================================================================
    - curtin in-target -- update-initramfs -u -k all

  user-data:
    runcmd:
      - systemctl disable cloud-init.service || true
      - systemctl disable cloud-config.service || true
      - systemctl disable cloud-final.service || true
    preserve_hostname: true
    manage_etc_hosts: true

  reporting:
    hook:
      type: print

  shutdown: reboot
